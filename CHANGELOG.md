# Changelog

All notable changes to Wish Code are recorded here.
Format: prompt-id-led entries (`feat(P-x): ...` / `docs(arch): ...`).

## Unreleased

- docs(arch): add M-0 wishcode inspection map at `docs/arch/M-0-inspection.md`
- docs(arch): add M-1 gap analysis at `docs/arch/M-1-gap-analysis.md`
- feat(shell): S-0 extract Wish Code shell into `src/shell/` (layout, chrome, navigation, state, settings, login, branding); replace monolithic `src/App.tsx` with re-export shim
- docs(arch): add S-0 ADR at `docs/arch/S-0.md`
- feat(ipc): D-0 typed-and-versioned `wish.*` IPC schemas — Zod schemas for all 60 channels organized by domain, IPC_PROTOCOL_VERSION + `wish:proto:version` handshake channel, IpcResult<T> envelope, WishErrorShape; ADR at `docs/arch/D-0.md`
- feat(ai): A-0 canonical AI types in `electron/shared/ai/` (canonical.ts, zod.ts) matching CONVENTIONS § 4; provider-neutral discriminated unions for content blocks + stream events; ADR at `docs/arch/A-0.md`
- chore: install `zod` + `vitest` as devDependencies
- feat(ipc): D-1 preload bridge migration — every response runtime-validated against the registry's response schema; lazy `wish:proto:version` handshake; legacy `{ok,value}` envelope still accepted until D-2; ADR at `docs/arch/D-1.md`
- test: vitest config + `npm test` script + 17 IPC tests (`registry.test.ts` 9 + `error.test.ts` 8)
- feat(ai): A-1 provider runtime — `ProviderAdapter` contract, `ProviderRegistry`, `ProviderRuntime` validating every yielded event through canonical Zod; `EchoAdapter` test provider; 7 runtime tests; ADR at `docs/arch/A-1.md`
- chore: total `npm test` count is now 24 passing (3 files)
- feat(agent): A-2 agent runtime in `electron/native/agent/` — `AgentRuntime` turn loop over A-1 `ProviderRuntime`, `InMemoryToolDispatcher`, canonical-only event surface, `AgentStopReason` (completed / max_turns / cancelled / error / unsupported_tool); 9 tests using a deterministic ScriptedAdapter cover happy path, two-turn tool round-trip, error flows, abort, max-turns; ADR at `docs/arch/A-2.md`
- chore: total `npm test` count is now 33 passing (4 files)
- feat(memory): Mem-0 multi-scope memory store at `electron/native/memory/` — six scopes (personal/session/workspace/team/task/agent) with required bindings validated at insert; `InMemoryMemoryStore` with put/get/update/remove/list/prune; deep-copy on get; insert-order tie-break for same-ms entries; 19 tests; ADR at `docs/arch/Mem-0.md`
- feat(telemetry): Tel-0 telemetry envelope at `electron/native/telemetry/` — `TelemetryEvent` (id/type/ts/level/redaction/schemaVersion/attributes/trace/source); `TelemetryEmitter` with auto-scrubbing of sensitive keys (password/token/secret/api_key/etc.); type-format validation (`<domain>.<noun>.<verb>`); pluggable `TelemetrySink` (sinks never throw); `MemorySink` for tests + dev; 14 tests; ADR at `docs/arch/Tel-0.md`
- chore: total `npm test` count is now 66 passing (6 files)
- feat(tasks): T-0 Task vs Job model at `electron/native/tasks/v2/` — `Task` (user intent, 6 statuses, origin + bindings) + `Job` (graph node, 7 statuses, dependencies, attempts, scratch state); forward-only transition validation; `cascadeSkip` walks reverse-dep tree; `readyJobs` is what the runner polls; 23 tests; ADR at `docs/arch/T-0.md`
- feat(knowledge): K-0 knowledge registry at `electron/native/knowledge/` — `KnowledgeSource` (kind/uri/scope/capabilities/status), `ProvenanceRef` shape, `InMemoryKnowledgeStore` with slug uniqueness + indexer lifecycle (unindexed → indexing → indexed/errored); 17 tests; ADR at `docs/arch/K-0.md`
- feat(capability): D-6 capability gate at `electron/native/capability/` — `CapabilityBroker` mediates 13 CONVENTIONS § 6 kinds; constraint matching for paths/hosts/providers/connectors/workspaces/tasks; `CapabilityDenied` carries code `capability.denied`; Tel-0-wired telemetry for grant.issued/revoked + check.allowed/denied without leaking the grant `reason` field; 16 tests; ADR at `docs/arch/D-6.md`
- chore: total `npm test` count is now 122 passing (9 files)
- feat(cells): Cell-0 manifest schema at `electron/native/cells/` — Zod-validated `CellManifest` with id/version/class/trustTier/capabilities/slots/deps/storage/signature; cross-field invariants (signature required iff trusted-signed; unique slot+entry pairs; unique dep ids); `parseManifest`, `manifestDeclaresCapability`, `slotContributionsFor`, `RESERVED_SHELL_SLOT_IDS`; 30 tests; ADR at `docs/arch/Cell-0.md`
- feat(tasks): T-1 `JobOrchestrator` + `RunnerRegistry` at `electron/native/tasks/v2/orchestrator.ts` — drives ready jobs through registered runners; concurrency cap; cascade-skip on failure; structured `runner.threw` and `orchestrator.no_runner` failures; Tel-0-wired telemetry (`job.run.started/succeeded/failed/no_runner`); `drain()` until fixed-point; 14 tests; ADR at `docs/arch/T-1.md`
- feat(memory): Mem-1 context assembly at `electron/native/memory/context.ts` — deterministic scoring (scope priority + pinned + substring + token overlap, recency tiebreak), char-budget guarantee with always-emit-at-least-one, `rankCandidates` + `renderEntry` helpers, scope-mix diagnostic; 18 tests; ADR at `docs/arch/Mem-1.md`
- chore: total `npm test` count is now 184 passing (12 files)
- feat(cells): Cell-1 registry — `InMemoryCellRegistry` keyed by `(id, version)`, semver-lite resolver (`*`/exact/`^`/`~`), `resolveDependencies` returns satisfied/missing split, `addFromRaw` forwards parse errors; 16 tests; ADR at `docs/arch/Cell-1.md`
- feat(cells): Cell-2 runtime — `CellRuntime` lifecycle FSM (inactive→activating→active→unloading→unloaded), capability pre-grant via D-6 broker, dep-resolution gate, per-class handler routing, Tel-0 events (`cell.activate.{started,succeeded,failed}`, `cell.deactivate.{succeeded,teardown_failed}`); 14 tests; ADR at `docs/arch/Cell-2.md`
- feat(activity): T-2 ActivityTimeline at `electron/native/activity/` — `TelemetrySink` that groups events by traceId, tracks highestLevel + domains, FIFO + oldest-evict caps, newest-first list with domain + minLevel filters; 10 tests; ADR at `docs/arch/T-2.md`
- feat(memory): Mem-2 surface adapters — `assembleChatContext` / `assembleCodeContext` / `assembleAgentContext` with surface-specific scope+binding defaults; bindings filter relaxed to "lenient" in `InMemoryMemoryStore` (only filter when entry actually carries the binding) so cross-scope queries work; 9 tests; ADR at `docs/arch/Mem-2.md`
- chore: total `npm test` count is now 233 passing (16 files)
- feat(cells): Cell-3 SDK at `electron/native/cells/sdk.ts` — `createCellSDK(host)` produces the only surface a Cell may import; capability checks gated by manifest declarations; memory pre-bound to `agent` scope + cell agentId; knowledge read-only with `cite()` emitting `provenance.ref.attached`; emit() scrubs sensitive attrs and stamps `source = "cell:<id>"`; slot disposers; isDisposed throws on use; 12 tests; ADR at `docs/arch/Cell-3.md`
- feat(knowledge): K-1 BM25 indexer at `electron/native/knowledge/indexer.ts` — paragraph/sentence-aware chunker (500 chars + 80 overlap), inverted index, BM25(k1=1.5, b=0.75) ranking with sourceIds + minScore filters; reindex replaces; `SearchHit.ref` is a ready-to-cite `ProvenanceRef`; markIndexed carries chunkCount + contentHash back to K-0; 18 tests; ADR at `docs/arch/K-1.md`
- feat(coagent): C-0 family scaffold at `electron/native/coagent/` — `COAGENT_FAMILY_ID`, 8 roles, 14 stable event kinds; typed pub/sub `CoAgentBus` with subscribe-filter delivery, last-writer-wins rejoin, family.member.joined/left lifecycle events; `CoAgentCore` joins as `core` with 256-event ring; thrown handlers caught + logged via Tel-0; 16 tests; ADR at `docs/arch/C-0.md`
- chore: total `npm test` count is now 280 passing (19 files)
- feat(cells): Cell-4 SlotHost at `electron/native/cells/slot-host.ts` — pure-logic registry of `(slot id) → ordered contributions`, priority + insertion-seq tiebreak, `unregisterCell`, optional strict mode for reserved shell slot ids, Tel-0 telemetry; 15 tests; ADR at `docs/arch/Cell-4.md`
- feat(knowledge): K-2 Retriever at `electron/native/knowledge/retrieval.ts` — joins K-1 BM25 with K-0 source filters (scope, status, slug allow/deny, source-id allow); stats for `totalIndexedSources/eligibleSources/indexHits/droppedByScore/droppedByPolicy`; ready-to-cite ProvenanceRef per hit; 11 tests; ADR at `docs/arch/K-2.md`
- feat(coagent): C-1 CoAgentTaskRuntime at `electron/native/coagent/task-runtime.ts` — joins C-0 family as `task`; bridges TaskStore (T-0) mutations to outbound bus events (`task.created/updated/deleted`); opt-in `applyPeerEvents` for inbound back-fill (Hermon mirror seam); attach/detach lifecycle; 13 tests; ADR at `docs/arch/C-1.md`
- chore: total `npm test` count is now 319 passing (22 files)
- feat(knowledge): K-3 provenance store at `electron/native/knowledge/provenance.ts` — append-only `ProvenanceRecord` (actionKind/actionId/author/refs[]/parent?), `latest()` index, `history()` walks parent chain newest-first, list filters by kind/id/author/since/until; 12 tests; ADR at `docs/arch/K-3.md`
- feat(cells): Cell-5 `CellGroup<TEvent>` at `electron/native/cells/groups.ts` — generalised typed pub/sub with knownKinds enforcement, last-writer-wins rejoin, thrown subscribers caught, namespaced Tel-0 events (`{groupId}.event_delivered/subscriber_threw/joined/left`); 14 tests; ADR at `docs/arch/Cell-5.md`
- feat(cells): Cell-6 `TrustVerifier` at `electron/native/cells/trust.ts` — three-tier trust policy (declarative / sandboxed / trusted-signed) with bundle SHA-256 check + pluggable `SignatureVerifier`; `InMemorySignatureVerifier` for tests + Cell Forge; structured `TrustVerdict` reasons; 13 tests; ADR at `docs/arch/Cell-6.md`
- chore: total `npm test` count is now 358 passing (25 files)
