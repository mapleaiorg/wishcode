/**
 * Barrel for the canonical AI types.
 *
 * Consumers import from `electron/shared/ai/` (or `@wish/protocol-types`
 * once the package exists in a later prompt).
 */

export * from './canonical.js';
export * as canonicalSchemas from './zod.js';
